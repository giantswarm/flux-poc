apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: gitops-app-sample-dev
  namespace: flux-app-dev
spec:
  releaseName: gitops-app-sample-dev
  chart:
    spec:
      chart: ./helm
      sourceRef:
        kind: GitRepository
        name: gitops-app-sample
        namespace: flux-app
      valuesFiles:
      - values.yaml
      - values-dev.yaml
      values:
        image:
          tag: "1fe03b52fb8d199a0a6aac347fc4641bce779c88"
  interval: 1m
