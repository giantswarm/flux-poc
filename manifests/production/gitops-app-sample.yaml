apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: gitops-app-sample-production
  namespace: flux-app-production
spec:
  releaseName: gitops-app-sample-production
  chart:
    spec:
      chart: ./helm
      sourceRef:
        kind: GitRepository
        name: gitops-app-sample
        namespace: flux-app
      valuesFiles:
      - ./values.yaml
      - ./values-production.yaml
  values:
    image:
      tag: "934355d25292c5b30b44e15435188e3e12bc8fbe"
  interval: 1m
